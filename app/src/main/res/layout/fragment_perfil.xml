<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="#F5F5F5"
    android:padding="16dp"
    android:gravity="center_horizontal"
    tools:context=".PerfilFragment">

    <!-- Título da Seção: "Editar Perfil" -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Editar Perfil"
        android:textSize="24sp"
        android:textColor="#333333"
        android:textStyle="bold"
        android:layout_marginTop="32dp"
        android:layout_marginBottom="32dp"/>

    <!-- Campo Nome -->
    <EditText
        android:id="@+id/edit_text_perfil_nome"
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:layout_marginBottom="16dp"
        android:hint="Nome"
        android:inputType="textPersonName"
        android:paddingStart="16dp"
        android:paddingEnd="16dp"
        android:textColorHint="#9E9E9E"
        android:textSize="16sp"
        android:background="@drawable/rounded_edittext_background"/>

    <!-- Campo E-mail (desabilitado para edição, pois é chave de login) -->
    <EditText
        android:id="@+id/edit_text_perfil_email"
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:layout_marginBottom="16dp"
        android:hint="E-mail"
        android:inputType="textEmailAddress"
        android:paddingStart="16dp"
        android:paddingEnd="16dp"
        android:textColorHint="#9E9E9E"
        android:textSize="16sp"
        android:background="@drawable/rounded_edittext_background"
        android:enabled="false" /> <!-- E-mail geralmente não é editável diretamente -->

    <!-- Campo Telefone -->
    <EditText
        android:id="@+id/edit_text_perfil_telefone"
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:layout_marginBottom="24dp"
        android:hint="Telefone"
        android:inputType="phone"
        android:paddingStart="16dp"
        android:paddingEnd="16dp"
        android:textColorHint="#9E9E9E"
        android:textSize="16sp"
        android:background="@drawable/rounded_edittext_background"/>

    <!-- Botão Trocar Senha (Visual Apenas) -->
    <Button
        android:id="@+id/button_trocar_senha"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Trocar senha"
        android:textColor="#333333"
        android:backgroundTint="#E0E0E0"
        android:layout_marginBottom="24dp"
        android:paddingStart="24dp"
        android:paddingEnd="24dp"/>

    <!-- Título Interesses -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Interesses"
        android:textSize="18sp"
        android:textColor="#333333"
        android:textStyle="bold"
        android:layout_marginBottom="16dp"/>

    <!-- Botão Selecionar Interesses (Visual Apenas) -->
    <Button
        android:id="@+id/button_selecionar_interesses"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Selecionar interesses"
        android:textColor="#333333"
        android:backgroundTint="#E0E0E0"
        android:layout_marginBottom="48dp"
        android:paddingStart="24dp"
        android:paddingEnd="24dp"/>

    <!-- Botão Salvar -->
    <Button
        android:id="@+id/button_salvar_perfil"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Salvar"
        android:textColor="#FFFFFF"
        android:backgroundTint="#3F51B5"
        android:textSize="18sp"
        android:textStyle="bold"
        android:textAllCaps="false"
        android:padding="14dp"/>

</LinearLayout>
